{
  "name": "paintball-manager-server",
  "version": "2.0.0",
  "private": true,
  "description": "Server locale del Paintball Manager: pagine web + relay WebSocket",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "ws": "^8.21.1"
  }
}
