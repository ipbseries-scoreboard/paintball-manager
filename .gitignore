node_modules/
.codex-backups/
# turn-config.js NON va ignorato: deve essere pubblicato con il sito, altrimenti
# i dispositivi che si collegano da fuori non ricevono la configurazione TURN.
# Il motivo per cui quella credenziale e' pubblica per forza e' spiegato in
# testa a turn-config.js; la protezione si fa col tetto di banda su metered.ca.
